{
  "name": "project",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "postinstall": "cd client && npm install && npm run build && cd .. && cd server && npm install express",
    "start": "node server/server.js"
  },
  "engines": {
    "node": "14.17.0"
  },
  "dependencies": {},
  "author": "",
  "license": "ISC"
}
